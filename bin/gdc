#!/bin/sh

if [ -e ~/.utcsname ] ; then
	utcsname=$(cat ~/.utcsname)
else
	read -p "Please enter your UTCS username: " utcsname
	echo $utcsname > ~/.utcsname
fi
ssh $utcsname@$(../gdc-get-low-load-host).cs.utexas.edu